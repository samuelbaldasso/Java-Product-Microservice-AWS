<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="application.name" source="spring.application.name"/>
    <springProperty scope="context" name="aws.region" source="aws.region" defaultValue="us-east-1"/>
    <springProperty scope="context" name="aws.log.group" source="aws.cloudwatch.log-group" defaultValue="/aws/application/${application.name}"/>
    
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- CloudWatch Logs Appender -->
    <appender name="CLOUDWATCH" class="ca.pjer.logback.AwsLogsAppender">
        <layout>
            <pattern>[%thread] [%date] [%level] [%logger] [%mdc] - %msg%n</pattern>
        </layout>
        <logGroupName>${aws.log.group}</logGroupName>
        <logStreamName>${HOSTNAME:-localhost}/${application.name}</logStreamName>
        <logRegion>${aws.region}</logRegion>
        <maxBatchLogEvents>50</maxBatchLogEvents>
        <maxFlushTimeMillis>30000</maxFlushTimeMillis>
        <maxBlockTimeMillis>5000</maxBlockTimeMillis>
        <retentionTimeDays>30</retentionTimeDays>
    </appender>
    
    <!-- Async CloudWatch Appender -->
    <appender name="ASYNC_CLOUDWATCH" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>500</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="CLOUDWATCH"/>
    </appender>
    
    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_CLOUDWATCH"/>
    </root>
    
    <!-- Application Specific Loggers -->
    <logger name="com.myapp" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
</configuration>